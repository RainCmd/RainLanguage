space native Print(string)

class SS
    public integer x
    public integer y
    public SS(integer x, integer y)
        this.x = x
        this.y = y
    public string Func()
        return (x+y).ToString()

public Main()
    SS ss = SS(123, 456)
    ss.x++
    Print("hellow world " + ss.Func())
